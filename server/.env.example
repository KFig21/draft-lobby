# Server config
PORT=4100
# Comma-separated if you have more than one (e.g. your Vercel prod domain +
# a preview deployment URL) — no spaces needed, they're trimmed.
CLIENT_ORIGIN=http://localhost:5183

# Canonical public URL of the app — the base for emailed links (password
# reset). Leave unset locally (falls back to the first CLIENT_ORIGIN). In
# production set it to your deployed web app's origin so reset links are always
# correct regardless of CLIENT_ORIGIN ordering.
# APP_URL=https://draft-lobby.vercel.app

# Supabase — from your project's Settings > API
SUPABASE_URL=https://YOUR_PROJECT.supabase.co
SUPABASE_SERVICE_ROLE_KEY=YOUR_SERVICE_ROLE_KEY
SUPABASE_ANON_KEY=YOUR_ANON_KEY

# Postgres connection string for running migrations (npm run db:migrate).
# Supabase → Settings → Database → Connection string (URI, includes password).
DATABASE_URL=postgresql://postgres:PASSWORD@db.YOUR_PROJECT.supabase.co:5432/postgres
